/** Popover option: opens the “new artist” modal instead of toggling an id. */
export const LIBRARY_ALBUM_ARTIST_CREATE_VALUE = '__create_new_artist__';

/** Clears all selected album artists (submit stays invalid until at least one is chosen). */
export const LIBRARY_ALBUM_ARTIST_NONE_VALUE = '__no_artists__';

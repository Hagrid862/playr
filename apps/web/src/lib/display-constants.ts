/** User-visible label when an album or track has no credited artists. */
export const UNKNOWN_ARTIST_LABEL = 'Unknown artist' as const;

/** User-visible label for the system album that holds tracks unlinked from deleted albums. */
export const UNKNOWN_ALBUM_LABEL = 'Unknown album' as const;
